<Window x:Class="FTP_Tool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:FTP_Tool.Models"
        xmlns:local="clr-namespace:FTP_Tool"
        Title="FTP File Monitor - Linux to Windows" Height="700" Width="1200" MinWidth="900" MinHeight="600"
        FontFamily="Segoe UI" Background="{DynamicResource PrimaryBackgroundBrush}" StateChanged="Window_StateChanged"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <!-- Resources are provided at Application level via App.xaml merged dictionaries -->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border x:Name="SidebarBorder" Grid.Column="0" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource PrimaryBorderBrush}" BorderThickness="0,0,1,0">
            <StackPanel>
                <!-- App Logo/Title -->
                <Border Background="{DynamicResource HeaderBackgroundBrush}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="FTP Monitor" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource InvertedTextBrush}"/>
                        <TextBlock Text="File Transfer Tool" FontSize="12" Foreground="{DynamicResource InvertedTextBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Navigation Menu -->
                <StackPanel Margin="0,20,0,0">
                    <Button x:Name="btnNavMonitor" Style="{StaticResource NavButtonActive}" Click="BtnNavMonitor_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📡" FontSize="16" Margin="0,0,12,0"/>
                            <TextBlock Text="Monitor" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="btnNavAlerts" Style="{StaticResource NavButton}" Click="BtnNavAlerts_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔔" FontSize="16" Margin="0,0,12,0"/>
                            <TextBlock Text="Alerts" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="btnNavSettings" Style="{StaticResource NavButton}" Click="BtnNavSettings_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0"/>
                            <TextBlock Text="Settings" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="btnNavAbout" Style="{StaticResource NavButton}" Click="BtnNavAbout_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,12,0"/>
                            <TextBlock Text="About" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Bottom Status -->
                <StackPanel VerticalAlignment="Bottom" Margin="20" Background="{DynamicResource CardBackgroundBrush}"
                           Height="100">
                    <Border CornerRadius="4" Padding="12" Background="{DynamicResource StatusInfoBackgroundBrush}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Ellipse x:Name="ellipseNavStatus" Width="8" Height="8" Fill="{DynamicResource InfoBrush}" 
                                         VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock x:Name="txtNavStatus" Text="Ready" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                            </StackPanel>
                            
                            <TextBlock x:Name="txtNavFiles" Text="No files monitored" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                            <TextBlock x:Name="txtNavRemote" Text="Remote: 0 files" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                            <TextBlock x:Name="txtNavErrors" Text="Errors: 0" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                            <TextBlock x:Name="txtNavLastCheck" Text="Last: Never" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                            <TextBlock x:Name="txtNavLastAlert" Text="Last alert: -" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Margin="20">
            <!-- Toggle button that appears on small windows (moved to top-right) -->
            <Button x:Name="btnToggleSidebar"  Visibility="Collapsed" Width="40" Height="36" HorizontalAlignment="Right" VerticalAlignment="Top" Click="BtnToggleSidebar_Click" ToolTip="Show menu" Margin="0,8,12,0" Panel.ZIndex="100" Background="Transparent" >
                <TextBlock Text="☰" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>

            <!-- Monitor Page (Default) -->
            <Grid x:Name="MonitorPage" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Page Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20" HorizontalAlignment="Center" MaxWidth="900">
                    <TextBlock Text="FTP Monitor" Style="{StaticResource PageTitle}" TextAlignment="Center"/>
                    <TextBlock Text="Monitor and download files from FTP server automatically" 
                   Style="{StaticResource PageSubtitle}" TextAlignment="Center"/>
                </StackPanel>

                <!-- Monitor Content - Responsive Grid -->
                <Grid Grid.Row="1" x:Name="MainContentGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" MinWidth="350" MaxWidth="500"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="1.5*" MinWidth="400"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Panel: Connection + Settings -->
                    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Disabled" Padding="0,0,8,0">
                        <StackPanel>
                            <!-- Connection Settings -->
                            <Border Style="{StaticResource ModernCard}">
                                <StackPanel>
                                    <TextBlock Text="Server Connection" Style="{StaticResource HeaderText}" 
                                   FontSize="16" FontWeight="SemiBold"/>

                                    <StackPanel Margin="0,12,0,0">
                                        <!-- Server & Port Row -->
                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Server Address" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                                <TextBox x:Name="txtHost" Style="{StaticResource ModernTextBox}" 
                                             ToolTip="FTP server hostname or IP address"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="Port" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                                <TextBox x:Name="txtPort" Text="21" Style="{StaticResource ModernTextBox}"
                                             ToolTip="FTP port (default: 21)"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Username -->
                                        <StackPanel Margin="0,0,0,12">
                                            <TextBlock Text="Username" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                            <TextBox x:Name="txtUsername" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>

                                        <!-- Password -->
                                        <StackPanel Margin="0,0,0,12">
                                            <TextBlock Text="Password" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                            <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernPasswordBox}"/>
                                        </StackPanel>

                                        <!-- Remote Folder & Test Button -->
                                        <Grid Margin="0,0,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Remote Path" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                                <TextBox x:Name="txtRemoteFolder" Text="/" Style="{StaticResource ModernTextBox}"
                                             ToolTip="Remote folder path (e.g., /incoming)"/>
                                            </StackPanel>

                                            <!-- Updated: Test button with clearer tooltip and small caption -->
                                            <StackPanel Grid.Column="2" Margin="0,20,0,0" HorizontalAlignment="Right">
                                                <Button x:Name="btnTestConnection" Click="BtnTestConnection_Click" 
                                                Style="{StaticResource SecondaryButton}" Padding="12,8"
                                                ToolTip="Test connection to FTP server. On success the host, username and password will be saved securely to Windows Credential Manager.">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🔗" Margin="0,0,6,0"/>
                                                        <TextBlock Text="Test"/>
                                                    </StackPanel>
                                                </Button>
                    <TextBlock Text="(Test also saves credentials if it succeeds)" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,6,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" Width="140" TextAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Download Settings -->
                            <Border Style="{StaticResource ModernCard}">
                                <StackPanel>
                                    <TextBlock Text="Download Settings" Style="{StaticResource HeaderText}" 
                                   FontSize="16" FontWeight="SemiBold"/>

                                    <StackPanel Margin="0,12,0,0">
                                        <!-- Local Folder -->
                                        <StackPanel Margin="0,0,0,12">
                                            <TextBlock Text="Local Download Folder" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="8"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox x:Name="txtLocalFolder" Grid.Column="0" Style="{StaticResource ModernTextBox}"/>
                                                <Button x:Name="btnBrowse" Grid.Column="2" Click="BtnBrowse_Click" 
                                            Style="{StaticResource SecondaryButton}" Padding="10,8" ToolTip="Browse Download Folder">
                                                    <TextBlock Text="📂"/>
                                                </Button>
                                            </Grid>
                                        </StackPanel>

                                        <!-- Check Interval -->
                                        <StackPanel Margin="0,0,0,12">
                                            <TextBlock Text="Check Interval" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox x:Name="txtInterval" Width="auto" MaxWidth="100" Text="30" 
                                             Style="{StaticResource ModernTextBox}"
                                             ToolTip="How often to check for new files (in seconds)"/>
                                                <TextBlock Text="seconds" Style="{StaticResource LabelText}" 
                                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Delete After Download -->
                                        <CheckBox x:Name="chkDeleteAfterDownload" 
                                      Content="Delete files after download" 
                                      FontSize="14" Margin="0,4,0,0"
                                      ToolTip="Remove files from server after successful download"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Control Buttons -->
                            <Border Style="{StaticResource ModernCard}">
                                <StackPanel>
                                    <TextBlock Text="Controls" Style="{StaticResource HeaderText}" 
                                   FontSize="16" FontWeight="SemiBold"/>

                                    <!-- Primary Actions -->
                                    <UniformGrid Columns="2" Rows="1" Margin="0,12,0,8">
                                        <Button x:Name="btnStart" Click="BtnStart_Click" 
                                    Style="{StaticResource ModernButton}" Margin="0,0,4,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="▶" Margin="0,0,6,0" FontSize="14"/>
                                                <TextBlock Text="Start Monitor"/>
                                            </StackPanel>
                                        </Button>

                                        <Button x:Name="btnStop" Click="BtnStop_Click" IsEnabled="False" 
                                    Style="{StaticResource DangerButton}" Margin="4,0,0,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="⏹" Margin="0,0,6,0" FontSize="14"/>
                                                <TextBlock Text="Stop"/>
                                            </StackPanel>
                                        </Button>
                                    </UniformGrid>

                                    <!-- Secondary Actions -->
                                    <UniformGrid Columns="3" Rows="1">
                                        <Button x:Name="btnCheckNow" Click="BtnCheckNow_Click" 
                                    Style="{StaticResource SecondaryButton}" Margin="0,0,4,0" Padding="8,8" ToolTip="Check Now">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="🔄" FontSize="14"/>
                                            </StackPanel>
                                        </Button>

                                        <Button x:Name="btnClearLog" Click="BtnClearLog_Click" 
                                    Style="{StaticResource SecondaryButton}" Margin="2,0" Padding="8,8" ToolTip="Clear Logs">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="🗑️" FontSize="14"/>
                                            </StackPanel>
                                        </Button>

                                        <Button x:Name="btnOpenLogFolder" Click="BtnOpenLogFolder_Click" 
                                    Style="{StaticResource SecondaryButton}" Margin="4,0,0,0" Padding="8,8" ToolTip="Open Logs Folder">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="📂" FontSize="14"/>
                                            </StackPanel>
                                        </Button>
                                    </UniformGrid>

                                    <!-- Status Bar -->
                                    <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="6" Padding="12" Margin="0,12,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock x:Name="lblStatus" Text="Ready" 
                                               Style="{StaticResource StatusText}" 
                                               FontWeight="SemiBold" FontSize="13"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Name="lblDownloaded" Text="0 files" 
                                               Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" FontSize="13"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Splitter -->
                    <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Center" 
                      VerticalAlignment="Stretch" Background="Transparent" 
                      ShowsPreview="True" Margin="4,0"/>

                    <!-- Right Panel: Status & Activity Log -->
                    <Grid Grid.Column="2" Margin="8,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Status Overview - Compact Design -->
                        <Border Grid.Row="0" Style="{StaticResource ModernCard}" Padding="16,14">
                            <StackPanel>
                                <TextBlock Text="Status Overview" Style="{StaticResource HeaderText}" 
                               FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" MinWidth="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Current Status with Indicator -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,8">
                                        <Ellipse x:Name="ellipseStatus" Width="12" Height="12" 
                                     Fill="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Text="Status:" FontWeight="SemiBold" 
                                       Style="{StaticResource LabelText}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="txtStatusText" 
                                   Text="Idle" Style="{StaticResource StatusText}" 
                                   FontWeight="SemiBold" Margin="0,0,0,8" VerticalAlignment="Center"/>

                                    <!-- Last Check -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Last check:" 
                                   FontWeight="SemiBold" Style="{StaticResource LabelText}" 
                                   Margin="22,0,0,8"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="txtLastCheck" 
                                   Text="-" Style="{StaticResource StatusText}" 
                                   Margin="0,0,0,8"/>

                                    <!-- Last Error -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Last error:" 
                                   FontWeight="SemiBold" Style="{StaticResource LabelText}" 
                                   Margin="22,0,0,0" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="txtLastError" 
                                   Text="-" Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap" 
                                   Style="{StaticResource StatusText}" MaxHeight="60"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>

                                    <!-- Last Alert (new) -->
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Last alert:" 
                                   FontWeight="SemiBold" Style="{StaticResource LabelText}" 
                                   Margin="22,0,0,0" VerticalAlignment="Top"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="txtLastAlert" 
                                   Text="-" Foreground="{DynamicResource WarningBrush}" TextWrapping="Wrap" 
                                   Style="{StaticResource StatusText}" MaxHeight="60"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Activity Log -->
                        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,8,0,0">
                            <DockPanel>
                                <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                                    <TextBlock Text="Activity Log" Style="{StaticResource HeaderText}" 
                                   FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <Button x:Name="btnJumpToLatest" HorizontalAlignment="Right" 
                                Visibility="Collapsed" Click="BtnJumpToLatest_Click" 
                                Style="{StaticResource SecondaryButton}" Padding="8,4"
                                FontSize="12">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="↓" Margin="0,0,4,0"/>
                                            <TextBlock Text="Latest"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>

                                <!-- Log ListBox with better styling -->
                                <Border BorderBrush="{DynamicResource SecondaryBorderBrush}" BorderThickness="1" CornerRadius="4" 
                            Background="{DynamicResource SecondaryBackgroundBrush}">
                                    <ListBox x:Name="lstLog" 
                                 ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DisplayedLogEntriesPublic}"
                                 ItemTemplate="{StaticResource LogEntryTemplate}" 
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 ScrollViewer.CanContentScroll="True"
                                 Background="Transparent"
                                 BorderThickness="0">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel />
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                    </ListBox>
                                </Border>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Settings Page -->
            <Grid x:Name="SettingsPage" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Page Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Settings" Style="{StaticResource PageTitle}"/>
                    <TextBlock Text="Configure application preferences and advanced options" Style="{StaticResource PageSubtitle}"/>
                </StackPanel>

                <!-- Settings Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="600" HorizontalAlignment="Left">
                        <!-- General Settings -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <TextBlock Text="🎛️ General Settings" Style="{StaticResource HeaderText}"/>
                                <CheckBox x:Name="chkAutoStart" Content="Start application with Windows" FontSize="14" Margin="0,0,0,12" ToolTip="Add or remove FTP Monitor from Windows startup. When enabled the app will be launched automatically when you sign in."/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock VerticalAlignment="Center" Text="Startup behavior:" Margin="0,0,12,0"/>
                                    <ComboBox x:Name="cmbStartupMode" Width="220" FontSize="14" ToolTip="Choose whether the app should open the main window or start minimized to tray when launched by Windows at login.">
                                        <ComboBoxItem Content="Open window"/>
                                        <ComboBoxItem Content="Start minimized to tray"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock VerticalAlignment="Center" Text="Theme:" Margin="0,0,12,0"/>
                                    <ComboBox x:Name="cmbTheme" Width="220" FontSize="14" ToolTip="Select application theme (Light or Dark)">
                                        <ComboBoxItem Content="Light"/>
                                        <ComboBoxItem Content="Dark"/>
                                    </ComboBox>
                                </StackPanel>
                                <CheckBox x:Name="chkMinimizeToTray" Content="Minimize to system tray when closed" FontSize="14" Margin="0,0,0,12" ToolTip="When enabled, closing the window hides the app to the system tray. Use the tray icon to restore or exit."/>
                                <CheckBox x:Name="chkStartMonitoringOnLaunch" Content="Start monitoring automatically when application starts" FontSize="14" Margin="0,0,0,12"/>
                                <!--<CheckBox Content="Show desktop notifications for downloads" FontSize="14" Margin="0,0,0,12"/>-->
                                <!--<CheckBox Content="Play sound when files are downloaded" FontSize="14"/>-->
                            </StackPanel>
                        </Border>

                        <!-- FTP Settings -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <TextBlock Text="⚡ FTP Settings" Style="{StaticResource HeaderText}"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Connection timeout:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBox x:Name="txtConnectionTimeout" Width="auto" MaxWidth="100" Text="30" Style="{StaticResource ModernTextBox}"/>
                                        <TextBlock Text="seconds" Style="{StaticResource LabelText}" Margin="10,0,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Max retry attempts:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <TextBox x:Name="txtMaxRetries" Grid.Row="1" Grid.Column="1" Width="auto" MaxWidth="100" Text="3" Style="{StaticResource ModernTextBox}" HorizontalAlignment="Left" Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Passive FTP mode:" Style="{StaticResource LabelText}" Margin="0,0,10,0"/>
                                    <CheckBox x:Name="chkUsePassiveMode" Grid.Row="3" Grid.Column="1" Content="Use passive mode (recommended)" FontSize="14"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- File Handling -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <TextBlock Text="📄 File Handling" Style="{StaticResource HeaderText}"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="File filters:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="*.txt;*.pdf;*.doc" Style="{StaticResource ModernTextBox}" Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Duplicate handling:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1" SelectedIndex="0" Margin="0,0,0,12">
                                        <ComboBoxItem Content="Skip existing files"/>
                                        <ComboBoxItem Content="Overwrite existing files"/>
                                        <ComboBoxItem Content="Rename with timestamp"/>
                                    </ComboBox>

                                    <CheckBox Grid.Row="2" Grid.ColumnSpan="2" Content="Create subfolders based on date" FontSize="14"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Logging Settings -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <TextBlock Text="📝 Logging Settings" Style="{StaticResource HeaderText}"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Write logs to file:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <CheckBox x:Name="chkLogToFile" Grid.Row="0" Grid.Column="1" Content="Enable writing logs to activity log file" FontSize="14" Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Minimum log level:" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <ComboBox x:Name="cmbMinimumLogLevel" Grid.Row="1" Grid.Column="1" Width="200" Margin="0,0,0,12">
                                        <ComboBoxItem Content="Debug"/>
                                        <ComboBoxItem Content="Info"/>
                                        <ComboBoxItem Content="Warning"/>
                                        <ComboBoxItem Content="Error"/>
                                    </ComboBox>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Log retention (days):" Style="{StaticResource LabelText}" Margin="0,0,10,12"/>
                                    <TextBox x:Name="txtLogRetentionDays" Grid.Row="2" Grid.Column="1" Width="80" Text="30" Style="{StaticResource ModernTextBox}" HorizontalAlignment="Left" Margin="0,0,0,12"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Max UI log lines:" Style="{StaticResource LabelText}" Margin="0,0,10,0"/>
                                    <TextBox x:Name="txtMaxLogLines" Grid.Row="3" Grid.Column="1" Width="120" Style="{StaticResource ModernTextBox}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Saved Credentials Management -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <TextBlock Text="🔐 Saved Credentials" Style="{StaticResource HeaderText}"/>
                                <TextBlock Text="Manage saved FTP credentials stored in Windows Credential Manager." Margin="0,0,0,8" Foreground="{DynamicResource SecondaryTextBrush}"/>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <ListBox x:Name="lstSavedCredentials" Grid.Row="1" MinHeight="100" Margin="0,8,0,8"
                                             Background="Transparent" BorderThickness="0"
                                             Foreground="{DynamicResource PrimaryTextBrush}"/>

                                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button x:Name="btnRefreshCredentials" Click="BtnRefreshCredentials_Click" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0">Refresh</Button>
                                        <Button x:Name="btnDeleteCredential" Click="BtnDeleteSavedCredential_Click" Style="{StaticResource DangerButton}">Delete Selected</Button>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Improved Alerts Page -->
            <Grid x:Name="AlertsPage" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Page Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,24">
                    <TextBlock Text="Alerts &amp; Notifications" Style="{StaticResource PageTitle}"/>
                    <TextBlock Text="Configure email notifications and alert schedules to stay informed about download activities" 
                   Style="{StaticResource PageSubtitle}" TextWrapping="Wrap"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="800">

                        <!-- Email Configuration Section -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <TextBlock Text="✉️" FontSize="18" VerticalAlignment="Center"/>
                                    <TextBlock Text="Email Configuration" Style="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </StackPanel>

                                <TextBlock Text="Configure SMTP settings for sending email notifications" 
                               Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13" Margin="0,-8,0,8"/>

                                <!-- SMTP Settings Grid -->
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- SMTP Host -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="SMTP Server:" Style="{StaticResource LabelText}" VerticalAlignment="Center"/>
                                    <TextBox x:Name="txtSmtpHost" Grid.Row="0" Grid.Column="1" 
                                 Style="{StaticResource ModernTextBox}" 
                                 ToolTip="Enter your SMTP server address (e.g., smtp.gmail.com)"/>

                                    <!-- Port and SSL in same row -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Port / SSL:" Style="{StaticResource LabelText}" VerticalAlignment="Center" Margin="0,12,0,0"/>
                                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,12,0,0">
                                        <TextBox x:Name="txtSmtpPort" Text="587" Style="{StaticResource ModernTextBox}" Width="80"
                                     ToolTip="Common ports: 25, 587 (TLS), 465 (SSL)"/>
                                        <CheckBox x:Name="chkSmtpSsl" Content="Enable SSL/TLS" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                    </StackPanel>

                                    <!-- Credentials Section -->
                                    <Border Grid.Row="2" Grid.ColumnSpan="2" 
                                Background="{DynamicResource StatusInfoBackgroundBrush}" CornerRadius="4" Padding="16" Margin="0,16,0,0">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Authentication" FontWeight="SemiBold" Margin="0,0,0,12"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Username:" Style="{StaticResource LabelText}" VerticalAlignment="Center"/>
                                            <TextBox x:Name="txtSmtpUser" Grid.Row="1" Grid.Column="1" Style="{StaticResource ModernTextBox}"/>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Password:" Style="{StaticResource LabelText}" VerticalAlignment="Center" Margin="0,12,0,0"/>
                                            <PasswordBox x:Name="txtSmtpPass" Grid.Row="2" Grid.Column="1" Style="{StaticResource ModernPasswordBox}" Margin="0,12,0,0"/>
                                        </Grid>
                                    </Border>

                                    <!-- From Address -->
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="From Address:" Style="{StaticResource LabelText}" VerticalAlignment="Center" Margin="0,16,0,0"/>
                                    <TextBox x:Name="txtEmailFrom" Grid.Row="3" Grid.Column="1" Style="{StaticResource ModernTextBox}" Margin="0,16,0,0"
                                 ToolTip="Email address that will appear as sender"/>

                                    <!-- Recipients Section -->
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Recipients:" Style="{StaticResource LabelText}" VerticalAlignment="Top" Margin="0,16,0,0"/>
                                    <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,16,0,0">
                                        <Border BorderBrush="{DynamicResource SecondaryBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8">
                                            <StackPanel>
                                        <TextBlock Text="Email Recipients" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                                                <ListBox x:Name="lstEmailRecipients" Height="80" BorderThickness="0" Background="Transparent"/>
                                            </StackPanel>
                                        </Border>

                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <TextBox x:Name="txtNewRecipient" Width="280" Style="{StaticResource ModernTextBox}" />
                                            <Button x:Name="btnAddRecipient" Click="BtnAddRecipient_Click" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="+" FontWeight="Bold" Margin="0,0,6,0"/>
                                                    <TextBlock Text="Add"/>
                                                </StackPanel>
                                            </Button>
                                            <Button x:Name="btnRemoveRecipient" Click="BtnRemoveRecipient_Click" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="−" FontWeight="Bold" Margin="0,0,6,0"/>
                                                    <TextBlock Text="Remove"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Test Buttons -->
                                    <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Left">
                                        <Button x:Name="btnTestEmail" Click="BtnTestEmail_Click" Style="{StaticResource SecondaryButton}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📧" FontSize="14"/>
                                                <TextBlock Text="Test Email Connection" Margin="6,0,0,0"/>
                                            </StackPanel>
                                        </Button>

                                        <Button x:Name="btnTestAlert" Click="BtnTestAlert_Click" Style="{StaticResource SecondaryButton}" Margin="12,0,0,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🚨" FontSize="14"/>
                                                <TextBlock Text="Send Test Alert" Margin="6,0,0,0"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Alert Schedule Section -->
                        <Border Style="{StaticResource ModernCard}">
                            <StackPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="⏰" FontSize="18" VerticalAlignment="Center"/>
                                        <TextBlock Text="Alert Schedule" Style="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="Configure when to receive alert notifications" Style="{StaticResource LabelText}" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Master Toggle -->
                    <Border Background="{DynamicResource StatusInfoBackgroundBrush}" BorderBrush="{DynamicResource SuccessBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,16,0,0">
                                    <CheckBox x:Name="chkAlertsEnabled" Content="Enable email alerts" FontSize="14" FontWeight="SemiBold"/>
                                </Border>

                                <Grid x:Name="pnlAlertScheduleContent">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Schedule Mode Selection -->
                                    <StackPanel Grid.Row="1" Margin="0,20,0,0">
                                        <TextBlock Text="Schedule Mode" Style="{StaticResource LabelText}" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                        
                                        <!-- Mode 1: 24/7 - ignores both days and hours -->
                                        <Border Background="{DynamicResource WarningBrush}" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="2" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                                            <StackPanel>
                                                <CheckBox x:Name="chkAlertAlways" FontWeight="SemiBold" FontSize="14">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🌍" Margin="0,0,8,0" FontSize="16"/>
                                                        <TextBlock Text="24/7 Mode — Always send alerts" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </CheckBox>
                                                <TextBlock Text="Alerts will be sent every day, all hours. All schedule settings below are ignored." 
                                                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Mode 2: All Day - respects selected days but ignores hours -->
                                        <Border Background="{DynamicResource NavButtonActiveBackgroundBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="2" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                                            <StackPanel>
                                                <CheckBox x:Name="chkAllDay" FontWeight="SemiBold" FontSize="14">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📅" Margin="0,0,8,0" FontSize="16"/>
                                                        <TextBlock Text="All Day Mode — Full day on selected days" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </CheckBox>
                                                <TextBlock Text="Alerts sent throughout the entire day on selected days below. Work shift times are ignored." 
                                                           FontSize="11" Foreground="{DynamicResource AccentBrush}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Mode 3: Custom Schedule - use shift-based timing -->
                                        <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource SecondaryBorderBrush}" BorderThickness="2" CornerRadius="6" Padding="14">
                                            <StackPanel>
                                                <CheckBox x:Name="chkUseMultiShift" FontWeight="SemiBold" FontSize="14">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="⏱️" Margin="0,0,8,0" FontSize="16"/>
                                                        <TextBlock Text="Custom Schedule — Use shift-based timing" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </CheckBox>
                                                <TextBlock Text="Define specific work shifts and break periods. Alerts sent only during active shifts on selected days." 
                                                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="28,6,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Days of week (hidden when 24/7 is enabled) -->
                                    <StackPanel Grid.Row="2" Margin="0,20,0,0" x:Name="pnlWeekdays">
                                        <TextBlock Text="Active Days" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <Border Background="{DynamicResource StatusInfoBackgroundBrush}" CornerRadius="4" Padding="16">
                                            <WrapPanel>
                                                <CheckBox x:Name="chkMon" Content="Monday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkTue" Content="Tuesday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkWed" Content="Wednesday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkThu" Content="Thursday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkFri" Content="Friday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkSat" Content="Saturday" MinWidth="110" Margin="0,0,12,8"/>
                                                <CheckBox x:Name="chkSun" Content="Sunday" MinWidth="110" Margin="0,0,12,8"/>
                                            </WrapPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Work Shifts & Excluded Intervals (shown only when Custom Schedule is enabled) -->
                                    <Border Grid.Row="3" x:Name="pnlShiftTables" Background="{DynamicResource StatusInfoBackgroundBrush}" CornerRadius="6" Padding="20" Margin="0,20,0,0">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                <TextBlock Text="⏰" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Shift Configuration" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBlock Text="Configure work shifts and break periods. Use 24-hour format (HH:mm). Supports overnight shifts (e.g., 22:00-06:00)." 
                                                       FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,16" TextWrapping="Wrap"/>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="1*"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="1*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Work Shifts DataGrid -->
                                                <StackPanel Grid.Column="0">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                        <TextBlock Text="✓" FontSize="14" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Active Work Shifts" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                        <TextBlock Text="Define when alerts should be active. Double-click to edit, press Tab to add new row." 
                                                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8" TextWrapping="Wrap"/>

                                                    <DataGrid x:Name="dgWorkShifts" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" 
                                                              HeadersVisibility="Column" Height="160" IsReadOnly="False" GridLinesVisibility="None" RowHeaderWidth="0"
                                                              VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                                              RowEditEnding="DgWorkShifts_RowEditEnding">
                                                        <DataGrid.Columns>
                                                            <DataGridTextColumn Header="Start Time" Binding="{Binding Start, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" Width="*">
                                                                <DataGridTextColumn.EditingElementStyle>
                                                                    <Style TargetType="TextBox" BasedOn="{StaticResource DataGridTextBoxEditingStyle}"/>
                                                                </DataGridTextColumn.EditingElementStyle>
                                                            </DataGridTextColumn>
                                                            <DataGridTextColumn Header="End Time" Binding="{Binding End, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" Width="*">
                                                                <DataGridTextColumn.EditingElementStyle>
                                                                    <Style TargetType="TextBox" BasedOn="{StaticResource DataGridTextBoxEditingStyle}"/>
                                                                </DataGridTextColumn.EditingElementStyle>
                                                            </DataGridTextColumn>
                                                        </DataGrid.Columns>
                                                    </DataGrid>

                                                    <Button x:Name="btnRemoveWorkShift" Click="BtnRemoveWorkShift_Click" Style="{StaticResource DangerButton}" Margin="0,8,0,0" HorizontalAlignment="Left">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="−" FontWeight="Bold" Margin="0,0,6,0"/>
                                                            <TextBlock Text="Remove Selected"/>
                                                        </StackPanel>
                                                    </Button>
                                                </StackPanel>

                                                <!-- Excluded Intervals DataGrid -->
                                                <StackPanel Grid.Column="2">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                        <TextBlock Text="✗" FontSize="14" Foreground="{DynamicResource DangerBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="Break/Excluded Periods" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                    <TextBlock Text="Times when alerts should be suppressed (lunch breaks, etc.). Double-click to edit." 
                                                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8" TextWrapping="Wrap"/>

                                                    <DataGrid x:Name="dgExcluded" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" 
                                                              HeadersVisibility="Column" Height="160" IsReadOnly="False" GridLinesVisibility="None" RowHeaderWidth="0"
                                                              VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                                              RowEditEnding="DgExcluded_RowEditEnding">
                                                        <DataGrid.Columns>
                                                            <DataGridTextColumn Header="Start Time" Binding="{Binding Start, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" Width="*">
                                                                <DataGridTextColumn.EditingElementStyle>
                                                                    <Style TargetType="TextBox" BasedOn="{StaticResource DataGridTextBoxEditingStyle}"/>
                                                                </DataGridTextColumn.EditingElementStyle>
                                                            </DataGridTextColumn>
                                                            <DataGridTextColumn Header="End Time" Binding="{Binding End, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" Width="*">
                                                                <DataGridTextColumn.EditingElementStyle>
                                                                    <Style TargetType="TextBox" BasedOn="{StaticResource DataGridTextBoxEditingStyle}"/>
                                                                </DataGridTextColumn.EditingElementStyle>
                                                            </DataGridTextColumn>
                                                        </DataGrid.Columns>
                                                    </DataGrid>

                                                    <Button x:Name="btnRemoveExcludedInterval" Click="BtnRemoveExcludedInterval_Click" Style="{StaticResource DangerButton}" Margin="0,8,0,0" HorizontalAlignment="Left">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="−" FontWeight="Bold" Margin="0,0,6,0"/>
                                                            <TextBlock Text="Remove Selected"/>
                                                        </StackPanel>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>

                                            <!-- Example shifts -->
                                            <Border Background="{DynamicResource NavButtonActiveBackgroundBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,16,0,0">
                                                <StackPanel>
                                                    <TextBlock Text="💡 Examples:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,6" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                    <TextBlock Text="• Single shift: 08:00-17:00" FontSize="11" Margin="0,0,0,3" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                    <TextBlock Text="• Split shifts: 08:00-12:00, 13:00-17:00 (lunch handled by breaks)" FontSize="11" Margin="0,0,0,3" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                    <TextBlock Text="• Night shift: 22:00-06:00 (crosses midnight)" FontSize="11" Margin="0,0,0,3" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                    <TextBlock Text="• Break example: 12:00-13:00 (lunch hour)" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </Border>

                                    <!-- Alert Threshold -->
                                    <StackPanel Grid.Row="4" Margin="0,20,0,0">
                                        <TextBlock Text="Download Activity Alerts" Style="{StaticResource LabelText}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <!-- Use theme brush so this panel respects dark/light themes -->
                                        <Border Background="{DynamicResource StatusInfoBackgroundBrush}" CornerRadius="4" Padding="16">
                                            <StackPanel>
                                                <CheckBox x:Name="chkSendDownloadAlerts" Content="Send alerts when no files are downloaded" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                                
                                                <StackPanel x:Name="pnlDownloadAlertSettings" Margin="24,0,0,0">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                                        <TextBlock Text="Alert threshold:" VerticalAlignment="Center"/>
                                                        <TextBox x:Name="txtAlertThreshold" Text="15" Style="{StaticResource ModernTextBox}" Width="auto" MaxWidth="80" Margin="12,0,0,0"/>
                                                        <TextBlock Text="minutes without activity" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                                    </StackPanel>

                                                    <CheckBox x:Name="chkSendAlertsWhenNotMonitoring" Content="Send alerts even when monitoring is stopped" 
                                                              ToolTip="When enabled, download alerts will continue even if monitoring is paused"/>

                                                    <!-- Editable alert email templates -->
                                                    <TextBlock Text="Alert Email Subject:" Style="{StaticResource LabelText}" Margin="0,16,0,4"/>
                                                    <TextBox x:Name="txtAlertEmailSubjectTemplate" Style="{StaticResource ModernTextBox}" ToolTip="Use placeholders: {threshold}, {status}, {host}, {remote}, {local}, {lastActivity}, {now}"/>
                                                    <TextBlock Text="Subject supports placeholders: {threshold}, {status}, {host}, {remote}, {local}, {lastActivity}, {now}" FontSize="11" Foreground="#605E5C" Margin="4,6,0,8"/>

                                                    <TextBlock Text="Alert Email Body:" Style="{StaticResource LabelText}" Margin="0,6,0,4"/>
                                                    <TextBox x:Name="txtAlertEmailBodyTemplate" Style="{StaticResource ModernTextBox}" Height="180" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" ToolTip="Body supports placeholders as listed above."/>

                                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                        <Button x:Name="btnPreviewAlertTemplate" Click="BtnPreviewAlertTemplate_Click" Style="{StaticResource SecondaryButton}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="🔍" Margin="0,0,6,0"/>
                                                                <TextBlock Text="Preview"/>
                                                            </StackPanel>
                                                        </Button>

                                                        <Button x:Name="btnRestoreAlertTemplates" Click="BtnRestoreAlertTemplates_Click" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="↻" Margin="0,0,6,0"/>
                                                                <TextBlock Text="Restore Defaults"/>
                                                            </StackPanel>
                                                        </Button>

                                        <TextBlock Text="Preview substitutes placeholders with current values." FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Help Text -->
                                    <Border Grid.Row="5" Background="{DynamicResource StatusInfoBackgroundBrush}" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,24,0,0">
                                        <StackPanel>
                                            <TextBlock Text="ℹ️ How Alert Schedules Work" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,12"/>
                                            

                                        <TextBlock Text="Schedule Modes:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,6" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <TextBlock Text="• 24/7 Mode: Alerts sent continuously, ignoring all day/time settings" FontSize="11" Margin="0,0,0,3"/>
                                            <TextBlock Text="• All Day Mode: Alerts sent throughout entire day on selected weekdays" FontSize="11" Margin="0,0,0,3"/>
                                            <TextBlock Text="• Custom Schedule: Alerts only during defined work shifts on selected days" FontSize="11" Margin="0,0,0,12"/>
                                            

                                            <TextBlock Text="Alert Types:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,6"/>
                                            <TextBlock Text="• Errors/Warnings: Sent immediately when they occur (respects schedule)" FontSize="11" Margin="0,0,0,3"/>
                                            <TextBlock Text="• Info Summaries: Batched and sent at configured interval" FontSize="11" Margin="0,0,0,3"/>
                                            <TextBlock Text="• Download Alerts: Triggered when no activity detected for threshold period" FontSize="11"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- About Page -->
            <Grid x:Name="AboutPage" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="500">
                    <TextBlock Text="ℹ️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="About FTP Monitor" Style="{StaticResource PageTitle}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Version 1.0.0" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="A powerful file monitoring and transfer application that helps you automatically download files from Linux FTP servers to your Windows machine." 
                               Style="{StaticResource PageSubtitle}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button Content="📧 Support" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                        <Button Content="🌐 Website" Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Floating in-window sidebar was moved into the main content Grid to ensure it slides over content area -->
            <Border x:Name="FloatingSidebar" Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource PrimaryBorderBrush}" BorderThickness="2"
                    Width="260" VerticalAlignment="Stretch" HorizontalAlignment="Left" Visibility="Collapsed"
                    RenderTransformOrigin="0.5,0.5" Panel.ZIndex="10" ClipToBounds="True"
                    CornerRadius="16">
                <Border.RenderTransform>
                    <TranslateTransform X="-260" />
                </Border.RenderTransform>
                <StackPanel>
                    <Border Background="{DynamicResource HeaderBackgroundBrush}" Padding="20" CornerRadius="16,16,0,0">
                        <StackPanel>
                            <TextBlock Text="FTP Monitor" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="File Transfer Tool" FontSize="12" Foreground="#E1F5FE"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Margin="0,20,0,0">
                        <Button x:Name="btnFloatingNavMonitor" Style="{StaticResource NavButtonActive}" Click="BtnNavMonitor_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📡" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Monitor" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnFloatingNavAlerts" Style="{StaticResource NavButton}" Click="BtnNavAlerts_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔔" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Alerts" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnFloatingNavSettings" Style="{StaticResource NavButton}" Click="BtnNavSettings_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnFloatingNavAbout" Style="{StaticResource NavButton}" Click="BtnNavAbout_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="About" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <!-- Bottom Status -->
                    <StackPanel VerticalAlignment="Bottom" Margin="20" Background="{DynamicResource CardBackgroundBrush}"
           Height="100">
                        <Border CornerRadius="4" Padding="12" Background="{DynamicResource StatusInfoBackgroundBrush}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Ellipse x:Name="ellipseFloatingNavStatus" Width="8" Height="8" Fill="{DynamicResource InfoBrush}" 
                         VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="txtFloatingNavStatus" Text="Ready" FontSize="12" FontWeight="SemiBold"/>
                                </StackPanel>

                                <TextBlock x:Name="txtFloatingNavFiles" Text="No files monitored" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                                <TextBlock x:Name="txtFloatingNavRemote" Text="Remote: 0 files" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                                <TextBlock x:Name="txtFloatingNavErrors" Text="Errors: 0" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                                <TextBlock x:Name="txtFloatingNavLastCheck" Text="Last: Never" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                                <TextBlock x:Name="txtFloatingNavLastAlert" Text="Last alert: -" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,1,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>